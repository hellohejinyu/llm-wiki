import chalk from 'chalk';
import type { Config } from '../types/index.ts';

export default async function lintCmd(config: Config, options: any) {
  console.log(chalk.yellow('lint command is not fully implemented yet in MVP.'));
}
